# Copy to .env and fill in. .env is gitignored — never commit real credentials.
DB_SERVER=ems-sql-dev-mihail.database.windows.net
DB_NAME=ems-dev-db
DB_USER=
DB_PASSWORD=
# Optional: table/view holding the readings. Leave blank to auto-discover.
DB_TABLE=
